import { PG } from "..";

import { Logger } from "@iac-factory/api-utilities";

export const Table = async function Table () {
    "CREATE TABLE IF NOT EXISTS "
}

export const Models = async function () {
    console.log("...")
}

export default Models;

Models().catch(console.log);

